{
  "description": "tld enrichment\nby Mosy\nv1.0.0",
  "processors": [
    {
      "registered_domain": {
        "field": "dns.question.name",
        "if": "ctx?.dns?.question?.name != null",
        "ignore_failure": true
      }
    },
    {
      "registered_domain": {
        "field": "url.domain",
        "if": "ctx?.url?.domain != null",
        "ignore_failure": true
      }
    }
  ]
}